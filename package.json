{
  "name": "localserve",
  "version": "0.0.1",
  "main": "src/index.js",
  "bin": {
    "localserve": "bin/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --bail=0",
    "clean": "rm -rf dist",
    "build": "ncc build src/index.js -o dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "author": "Geoff Miller",
  "license": "MIT",
  "description": "A dead-simple local dev server for quickly mocking API endpoints.",
  "private": false,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/geoffmiller/localserve.git"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3",
    "jest": "^29.7.0"
  }
}
